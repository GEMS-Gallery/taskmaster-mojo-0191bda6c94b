type Task = 
 record {
   completed: bool;
   completionDate: opt int;
   description: text;
   id: nat;
 };
service : {
  addTask: (text) -> (nat);
  completeTask: (nat) -> ();
  deleteTask: (nat) -> ();
  getTasks: () -> (vec Task) query;
}
